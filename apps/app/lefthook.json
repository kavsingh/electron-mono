{
	"$schema": "../../node_modules/lefthook/schema.json",
	"pre-commit": {
		"jobs": [
			{
				"root": "apps/app",
				"glob": "*.{ts,tsx,js,jsx}",
				"run": "pnpm oxlint --no-error-on-unmatched-pattern {staged_files}"
			}
		]
	}
}
